import { describe,expect,it } from "vitest";import { SqlitePlatformStore } from "../../adapters/ontology-store-sqlite/src/index.js";import { OntologyPlatform } from "../../packages/application/src/index.js";import { MCP_TOOLS,OntologyMcpAdapter } from "../../packages/mcp-server/src/index.js";import { validSnapshot } from "../fixtures-v3.js";
describe("MCP adapter",()=>{it("exposes the eight frozen tools with JSON Schema",()=>{expect(MCP_TOOLS.map(t=>t.name)).toEqual(["ResolveOntologyContext","ExecuteSemanticQuery","ContinueSemanticQuery","GetOntologySnapshot","ApplyOntologyDraftPatch","ValidateOntologyDraft","PublishOntologyDraft","ExplainInference"]);expect(MCP_TOOLS.every(tool=>tool.inputSchema&&typeof tool.inputSchema==="object")).toBe(true);});it("delegates context resolution to the same application",async()=>{const store=new SqlitePlatformStore(":memory:");store.savePublished(validSnapshot());const app=new OntologyPlatform(store);const result:any=await new OntologyMcpAdapter(app).callTool("ResolveOntologyContext",{namespace:"retail",question:"销售额",purpose:"PLAN"});expect(result.ontologyVersion).toBe(1);expect(result.metrics[0].id).toBe("m_sales");});});
