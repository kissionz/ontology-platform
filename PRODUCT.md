# Product

## Register

product

## Users

数据产品经理、数据工程师、分析工程师与外部 Agent 集成开发者。他们在高信息密度的桌面工作台中维护业务语义、审查发布质量，并以稳定协议执行问数。

## Product Purpose

提供独立、开放、可版本化的本体控制面与语义执行内核。成功意味着同一版本与输入始终得到相同的语义候选、推论、查询计划和证据，明确问数可在一次外部调用中完成。

## Brand Personality

克制、精确、可信。界面应让专业用户专注于定义、依赖和证据，并清楚感知当前版本与执行边界。

## Anti-references

避免营销落地页式的夸张视觉、装饰性插画、玻璃拟态、低密度大卡片、不可解释的自动推理，以及把未完成能力伪装成可用结果的模拟数据。

## Design Principles

1. 版本与来源始终可见，任何语义结果都能定位到事实、规则与证据。
2. 复杂度通过稳定的信息架构和渐进详情管理，不隐藏关键治理信息。
3. 标准交互保持熟悉，颜色只表达选择、动作和状态。
4. 已发布内容只读，编辑、校验、发布的状态边界明确。
5. 缺失运行条件时给出可操作错误，不以静态样例冒充真实能力。

## Accessibility & Inclusion

以 WCAG 2.1 AA 为基线，支持键盘导航、清晰焦点、语义化状态、非颜色唯一编码和 reduced-motion。桌面基准为 1600 × 1000，最低支持 1280 像素宽度。

## 界面语言与当前验收范围

2026-09-05 用户确认：公理页面使用中文规则名称、业务说明、类型与生效阶段；PROPERTY 显示为“属性”，ENTITY 显示为“实体”，其余术语按相同原则呈现。

当前阶段以代码、契约、测试夹具和浏览器流程验收为准，暂不进行 SelectDB 正式业务内容验证。

平台密钥默认自动生成，包括管理员 API Key、客户端 API Key 与数据源凭据加密密钥；管理员和加密密钥持久化并在重启时复用。

平台支持从空库开始建模：数据源配置与 Schema 扫描独立可用，用户可创建空白草稿、从物理表添加对象并发布首个版本；属性值索引在本体发布后启用。

数据源中的表列表保持简洁，仅显示表名称和已有表注释；操作结果使用简短中文提示。

本体工作台参考 InsightFlow 的结构：顶部统计、左侧目录、中间对象定义与编辑标签、右侧待建模表与发布操作。对象编辑分为基本信息、属性、关系、规则；对象类型、语义类型、可见性和聚合方式使用中文描述。新建时选择对象类型，删除在草稿中处理并说明依赖影响。

属性编辑时，选择金额类型默认填入 CNY、可加、求和；选择编码、名称、类别、地理位置语义时默认开启值检索（仅非敏感且可用于分析的属性）。默认值可手动修改，保存后保留用户设置。

关系编辑支持七类关系及组成关系专属设置。配置通过 Kernel 1.2.0 生成可追溯公理并约束发布、可达路径与查询输出；必选关系控制内连接，组成策略控制子对象汇总或存在性筛选，派生关系用于无环血缘说明。关系变更随对象保存，发布后生效。

系统管理提供 API、MCP 和 SDK 接入说明。API 调试台随接口切换展示中文用途、权限、参数、返回状态和请求示例；参数说明取自 OpenAPI，无请求体的接口使用紧凑表单。MCP 展示实际八个工具与调用结构，SDK 提供 TypeScript、Python 方法及可复制示例，说明版本固定、权限、分页和错误处理。

基本信息展示业务描述、分类、负责人、同义词和行级粒度；属性包含字段映射、唯一性、敏感性、数字聚合规则和实体引用。语义匹配优先级、值检索和导出范围保留在高级设置。问数会话、图表及回答呈现配置由消费端管理。
