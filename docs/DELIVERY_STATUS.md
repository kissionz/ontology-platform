# 阶段交付状态

更新日期：2026-09-06。需求基线为 `IMPLEMENTATION_SPEC.md`，当前验收范围按用户在 2026-09-05 的补充要求执行：暂不进行 SelectDB 正式业务内容验证。

本次交付为核心平台与控制台可运行基线，包含 Phase 0–6 的实现以及 Phase 7 的本地运维和验证能力。最终 Definition of Done 尚未全部关闭；测试夹具的通过结果不代表真实业务库验收完成。

## 当前验证证据

- TypeScript strict 和生产 build 通过。
- 38 个测试文件、207 项单元/契约/集成测试通过。
- 20 项 Chromium E2E 通过，包含对象类型、中文聚合设置保存、引用关系保存、草稿删除、未保存保护、空库从数据源配置到首次发布，以及六页面 1600/1280 两种宽度。
- A01–A12、Q01–Q14、C01–C06 有对应测试；U01–U12 有浏览器工作流。`tests/console` 的源码检查仅作为补充。
- 空数据库启动、备份恢复、真实参考 SQLite 读取、事务回滚与幂等导入已验证。
- P95 与 1,000 对象/5,000 关系测试通过。当前性能数据来自测试夹具，规模测试的关系拓扑为重复边压力场景，不能代表所有生产拓扑。

## 已交付能力

| 阶段 | 实现与证据 |
|---|---|
| Phase 0 | Workspaces、strict 类型检查、共享 Zod、生成 OpenAPI、SQLite migrations |
| Phase 1 | v2→v3、稳定 ID 与内容比对、发布版本/草稿分离导入、整批事务回滚、物理表和值索引状态 |
| Phase 2 | 内建公理、发布校验、安全方向关系闭包、层级与比例推论、确定性证据 |
| Phase 3 | 按业务概念分类检索候选、未命中与歧义反馈、必要关系路径、指标依赖、业务值绑定、短引用、会话固定版本、敏感定义过滤 |
| Phase 4 | 迁移 Query IR 编译器与 SQL Guard、AUTO/FIXED_SHAPE、时间范围与比较、分页一致性、版本隔离的形状记录 |
| Phase 5 | HTTP、认证、Scope、限流、请求审计、幂等写入、MCP HTTP 适配、TypeScript/Python SDK |
| Phase 6 | 六页面工作台、图谱 Inspector、草稿批量变更、发布/diff、数据源配置、索引、OpenAPI 调试、客户端权限 |
| Phase 7 | 健康/指标、结构化日志、加密凭据、备份恢复、迁移/接入文档、本地测试与浏览器验证 |

## 本轮加固

- 快照、发布说明、草稿删除和发布审计处于同一个事务。
- 导入不再将旧草稿当作发布版本；任一版本或索引非法时整批回滚。
- full Context、HTTP/MCP 导出和短引用目录统一过滤敏感属性。
- 会话省略版本或指定 latest 时继续使用会话固定版本。
- 数据源凭据使用绑定 sourceId 的 AES-256-GCM 加密，保存配置后重建连接。
- 生产 MCP 经过与 HTTP 相同的认证、Scope、限流和审计。
- 公理关系闭包覆盖多跳路径；AUTO 业务值作为 SQL 参数传递。
- 写入支持进程内 10 分钟 Idempotency-Key 重试窗口；变更请求体复用同一 key 返回冲突。
- 发布后异步构建值索引，索引失败不会回滚已发布本体，并暴露失败状态。

- 澄清待办持久化 30 分钟，保留原候选与版本，重启可继续；失败可重试，成功后消费。
- FIXED_SHAPE 的 DIRECT 筛选复用持久化 SQL 模板，动态参数不写入模板；数组参数数量、物理 Schema 或版本变化会重新编译。
- 公共查询契约接通高级计算，共享 Zod、OpenAPI、MCP schema；HTTP 测试覆盖窗口占比、组内排名、组合条件、时间比较、累计与逐期条件。
- 发布前展示 Golden Cases 运行时间、revision、摘要与逐项结果；报告持久化并关联发布审计，发布重新校验当前草稿，未配置用例明确标记。
- 公理页面提供中文名称、类型、说明、参数和证据，支持全部公理域、对象与版本筛选及定义跳转。
- 概览详情标签页和指标节点可操作；回滚草稿可以进入编辑、离开后继续编辑；物理字段映射受扫描 Schema 约束，索引重建可指定历史版本。
- 管理员 API Key 与凭据加密密钥首次启动自动生成、持久化并在重启后复用，本机 MCP 自动读取；客户端密钥创建时自动生成。
- 空库支持独立配置数据源和扫描 Schema、创建空白草稿、从物理表添加对象并发布 v1；初始草稿并发发布检查版本冲突，值索引状态局部展示，不阻塞数据源管理。
- Schema 扫描使用显式关联条件和字段所属表别名，避免 SelectDB 对 `TABLE_SCHEMA`、`TABLE_NAME` 的歧义解析；数据库名称仍通过参数绑定。
- 数据表列表展示名称与表注释，扫描和连接结果使用中文短提示；搜索与筛选控件字号保持一致。
- 本体编辑参考 InsightFlow 的统计、目录、定义、待建模区结构，提供基本信息、属性、关系、规则标签页。业务描述、分类、负责人、同义词、粒度、唯一性、敏感性、数字类型、可加性和聚合方式可编辑；新增对象支持五种类型，公理按类型生成。
- 草稿删除对象时展示并清理依赖的指标、关系和层级；未保存的对象修改在离开页面时提示，校验发布要求先保存。Kernel 1.1.0 补齐事件标识与关联对象引用数量的公理实例。
- 增加 GitHub Actions build、测试和 Chromium E2E 流程，截图路径兼容 macOS/Linux。

## 尚待关闭

1. **业务环境验收暂缓**：按用户确认，本轮不接入 SelectDB 正式内容，不将其列为当前交付阻塞。现有参考 SQLite 只有空 v0，实际业务迁移与数值口径验收留待后续安排。
2. **高级查询能力**：FIXED_SHAPE 已开放窗口/分组、周期条件、时间比较、派生计算、递归层级筛选和组合条件；普通 DIRECT 筛选已使用持久化 SQL 模板缓存，时间表达式和高级参数布局仍走完整编译。澄清待办已持久化并验证重启恢复。
3. **验收边界**：复杂指标、关系和层级定义保留结构化 JSON 编辑；Golden Cases 验证编译与 SQL 结构，正式业务结果验收按用户要求暂缓。

本轮继续关闭代码与交互验收项；正式业务数据验收作为后续独立阶段。

- 关系编辑与 Kernel 1.2.0 已接通：七类关系专属公理、字段映射与方向基数校验、组成汇总策略、派生血缘，查询返回实际关系证据。新增测试覆盖发布后内连接改变真实结果、历史版本隔离、EXISTS、错误类型与循环阻止发布。

- API 调试台已覆盖 29 个操作的用途、权限、参数、返回和示例；MCP 八个工具及 TypeScript/Python SDK 接入指南同步页面和仓库文档，示例通过契约校验。

- 统一检索对象、属性、指标和值索引：主名称归并、对象优先级与属性覆盖、指定范围绑定、同优先级澄清，以及关联路径约束已接通。默认响应提供业务绑定摘要，详细定义按 projection 显式返回；API、MCP 和 SDK 说明同步。测试覆盖绑定用于查询、同名实体按身份分组、权限和版本边界。

- INTENT 一体化业务查询已接通 API、MCP 和 SDK。测试覆盖多跳关联、年度筛选、实体名称分组、排序、缺失信息阻止执行，以及澄清状态在实例重建和发布新版本后继续使用原版本。

- API 说明独立展示，覆盖 29 个接口的公共与业务响应字段，查询接口提供成功、缺失信息和澄清示例；调试台默认请求按操作同步。浏览器遍历验证全部接口示例，检查独立文档页与 1600/1280 布局。

- 图谱支持中心节点自动布局与交叉优化，长按拖动、位置本地保存、刷新恢复和重置布局。布局单测及浏览器拖动流程通过，两种桌面宽度截图已检查。

- 概览详情栏修复长表名挤压布局，指标公式和关联端点显示业务名称，技术定义折叠保留。已验证长表名、名称替换与两种宽度截图。

- 关系类型 Mock 专项验证完成：七类关系覆盖公理、发布限制和实际 SQL 结果；详见 [关系类型验证报告](./RELATION_TYPE_MOCK_REPORT.md)。发现 EXISTS_ONLY 尚未接通 INTENT 筛选，以及业务数据实际唯一性不自动校验；本轮仅记录，未修改运行时。
